@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    font-family: 'Nirmala UI';
    @apply h-screen w-screen;
  }

  svg {
    @apply inline-block;
  }
}
.cursor-button {
  @apply cursor-default sm:cursor-pointer;
}
